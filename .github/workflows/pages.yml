name: Deploy

on:
  push:
    branches: [main]
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: pages
  cancel-in-progress: false

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7
      - uses: actions/setup-node@v7
        with:
          node-version: 24
          cache: npm
      # nothing ships that the data, map or css checks would refuse
      - run: node scripts/check-data.mjs
      - run: node scripts/check-map.mjs
      - run: node scripts/check-css.mjs
      - run: npm ci
      # Pages serves this project under /museo/
      - run: BASE_PATH=/museo npm run build
      - name: Refuse to publish font files
        run: |
          if find build -iname '*.woff*' -o -iname '*.otf' -o -iname '*.ttf' | grep -q .; then
            echo "::error::a font file reached build/ — this site is set in system type and ships none"
            exit 1
          fi
      - uses: actions/configure-pages@v6
      - uses: actions/upload-pages-artifact@v5
        with:
          path: build

  deploy:
    needs: build
    runs-on: ubuntu-latest
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    steps:
      - id: deployment
        uses: actions/deploy-pages@v5
